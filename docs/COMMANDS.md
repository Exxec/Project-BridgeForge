# BridgeForge commands

Generated by `bridgeforge docs-index` from the argument parser. Do not edit by hand: `tests/test_checks_index.py` fails when this file is stale.

118 commands and subcommands. Run as `python -m bridgeforge <command> ...`; exit codes: 0 ok, 1 an attention status, 2 a usage or input error.

| Command | What it does |
|---|---|
| [`scan`](#scan) | scan a mod directory without modifying it |
| [`bytecode-inspect`](#bytecode-inspect) | inspect class/JAR symbolic references without rewriting |
| [`bytecode-diff`](#bytecode-diff) | compare class/JAR symbolic and structural inventories |
| [`bytecode-plan`](#bytecode-plan) | produce review-only exact bytecode remap candidates |
| [`bytecode-apply`](#bytecode-apply) | apply exact approved bytecode remaps to an output .class copy |
| [`workspace`](#workspace) | create an immutable-reference workspace and working copy |
| [`plan`](#plan) | produce an explicit migration plan for a workspace |
| [`apply`](#apply) | apply only explicitly approved planned rules |
| [`rollback`](#rollback) | restore a working copy from a checkpoint |
| [`build-plan`](#build-plan) | model a compile environment without running javac |
| [`compile`](#compile) | run the explicit workspace build profile |
| [`package-jar`](#package-jar) | package successful workspace classes into a reviewable JAR output copy |
| [`compile-feedback`](#compile-feedback) | link compiler evidence to planned migration candidates |
| [`review-bundle`](#review-bundle) | create a bounded human/agent review handoff |
| [`validate`](#validate) | run workspace-integrity and structural validation |
| [`save-risk`](#save-risk) | flag changed persistent-identifier-shaped fields |
| [`pipeline`](#pipeline) | run the auditable Bridgeforge modernization pipeline |
| [`packs`](#packs) | list bundled migration packs |
| [`migration-pack-candidate`](#migration-pack-candidate) | create a non-applying evidence contract for a proposed library mapping |
| [`runtime-profile`](#runtime-profile) | record an explicit opt-in runtime launch profile |
| [`runtime-smoke`](#runtime-smoke) | inspect or explicitly run a runtime profile |
| [`inspect`](#inspect) | show workspace plans, diffs, and checkpoints |
| [`export-patch`](#export-patch) | export a patch-only package |
| [`opportunities`](#opportunities) | report non-applying library-adoption opportunities |
| [`doctor`](#doctor) | check local tooling, packs, and optional workspace integrity |
| [`conflicts`](#conflicts) | detect planned-edit and duplicate-class conflicts |
| [`provenance`](#provenance) | write deterministic workspace and artifact hashes |
| [`corpus-compare`](#corpus-compare) | compare an explicitly selected local mod to a sanitized baseline |
| [`corpus-audit`](#corpus-audit) | write a read-only aggregate report for explicit mod directories |
| [`cross-mod-analyze`](#cross-mod-analyze) | build a read-only dependency, class, and campaign-ID graph for explicit mod directories |
| [`campaign-identity-inventory`](#campaign-identity-inventory) | inventory source-defined campaign IDs from explicit mod directories |
| [`campaign-identity-check`](#campaign-identity-check) | resolve campaign literal lookups against an explicit identity inventory |
| [`decompiler-review`](#decompiler-review) | record or explicitly run a user-supplied decompiler as untrusted review evidence |
| [`release-lineage`](#release-lineage) | compare an explicitly ordered sequence of mod releases without modifying them |
| [`archive-preflight`](#archive-preflight) | inspect a ZIP archive without extracting it |
| [`archive-stage`](#archive-stage) | extract a preflight-safe ZIP into a new explicit destination |
| [`library-api-inventory`](#library-api-inventory) | inventory class symbols in a supplied local library JAR |
| [`library-api-match`](#library-api-match) | compare a mod's imports with a supplied local library API inventory |
| [`dependency-api-check`](#dependency-api-check) | compare direct optional-mod imports with explicit local API inventories |
| [`release-evaluate`](#release-evaluate) | compare two release directories without modifying either |
| [`revival-audit`](#revival-audit) | audit final revival evidence and optionally attest its release ZIP |
| [`working-tree-layout`](#working-tree-layout) | classify generated, backup, and source/content candidates without changing a mod |
| [`source-authority`](#source-authority) | record an explicit selected source root for a working tree |
| [`build-input-manifest`](#build-input-manifest) | record source authority and annotation-processing build evidence |
| [`integration-scenarios`](#integration-scenarios) | suggest review-only runtime scenarios from optional integration evidence |
| [`log-triage`](#log-triage) | classify a Starsector log into FATAL / MOD-ERROR / KNOWN-NOISE / OTHER without modifying it |
| [`copy-drift`](#copy-drift) | hash-compare a mod working copy against its deployed/test-rig copy |
| [`jar-audit`](#jar-audit) | compare a rebuilt mod jar with its original for bundled libraries, removed classes, and reflection use |
| [`build-tag`](#build-tag) | iterate a visible build tag in a revived mod's mod_info.json name/version, surgically (never re-serializes the file) |
| [`translate-export`](#translate-export) | export every non-English (CJK) player-visible string with a stable id (read-only; handles Starsector's lenient CSV/JSON and jar strings) |
| [`translate-apply`](#translate-apply) | write a filled translation JSON into a copy of the mod (or an explicit working copy with --in-place) |
| [`translate-tm`](#translate-tm) | write a translation JSON's pairs as Project Go (SSMT) translation memory, for `ssmt tm import <db> json <file>` |
| [`translate-check`](#translate-check) | report leftover non-English strings and designTypeColors keys that no tech/manufacturer uses |
| [`fix`](#fix) | apply a SAFE, mechanical fixer for one supported finding id (dry-run diff by default) |
| [`prepare-test`](#prepare-test) | sync a rig test copy from a working copy, and optionally run a boot test |
| [`boot-test`](#boot-test) | run a boot smoke test of a Starsector runtime with explicit mods enabled |
| [`compat-set`](#compat-set) | install a named standard compatibility pack (roadmap P4) into a test rig |
| [`compat-set install`](#compat-set-install) | copy a named compat set's mods from a source mods directory into a rig's mods/ |
| [`dossier`](#dossier) | build a size-capped agent triage dossier for a mod (index + parts) |
| [`build-probe-mod`](#build-probe-mod) | compile the bridgeforge-probe in-game probe mod (roadmap P3) and pack it deterministically |
| [`probe-config`](#probe-config) | write bf_probe_config/bf_probe_rig into a test rig's saves/common, and optionally install the probe mod |
| [`save-compat`](#save-compat) | check whether a save's referenced mod classes exist in the build's loaded jars (read-only) |
| [`save-inspect`](#save-inspect) | stream a save and report tracked mod state, known-list sizes and per-mod object counts (read-only) |
| [`save-diff`](#save-diff) | compare tracked mod state between two saves, e.g. campaign.xml vs campaign.xml.bak (read-only) |
| [`save-scripts`](#save-scripts) | count a mod's scripts/listeners per holder in a save and flag duplicates (read-only) |
| [`save-growth`](#save-growth) | per-mod object counts and file sizes across a save chain, with a growth warning (read-only) |
| [`save-provenance`](#save-provenance) | compare the mod versions/BF build tags a save was made with against current working copies (read-only) |
| [`save-content`](#save-content) | check the data ids a save stores (hulls, variants, weapons, factions, ...) against the build plus vanilla (read-only) |
| [`save-removal`](#save-removal) | INTERNAL ONLY: everything a save still depends on for one mod; SAFE_TO_REMOVE or UNSAFE (read-only) |
| [`save-summary`](#save-summary) | write a redacted, shareable save summary with no player data (local file only) |
| [`bootstrap`](#bootstrap) | one-time: write a scan baseline and record the current build manifest for each mod (nothing written inside mods) |
| [`who-locks`](#who-locks) | read-only process/Restart Manager diagnosis; never stops processes |
| [`intake`](#intake) | preserve a ZIP and create a fresh convention-layout assessment copy |
| [`board`](#board) | read-only declared-evidence status board; missing evidence remains unknown |
| [`promote`](#promote) | stage/audit a release and retain prior builds; dry-run by default |
| [`rig-doctor`](#rig-doctor) | pre-flight checks for a test rig: isolation, running game, probe install, enabled mods, working-copy drift, real-install saves untouched |
| [`rig-create`](#rig-create) | register a dedicated historical Starsector install as a P10 reference rig |
| [`test-plan`](#test-plan) | list the minimal live tests and probe assertions for what changed since a build tag (read-only) |
| [`perf-gate`](#perf-gate) | summarise an SPW performance report and log spam; PASS/WARN/FAIL against thresholds (report-only by default) |
| [`release`](#release) | run the release gates and, with --apply and all gates passing, package the mod (dry run by default) |
| [`save-snapshot`](#save-snapshot) | tag, list and restore rig-only save copies (never touches a real install) |
| [`save-snapshot tag`](#save-snapshot-tag) | copy <rig>/saves/<save> into <rig>/save_snapshots/<tag>/ with a hash manifest |
| [`save-snapshot list`](#save-snapshot-list) | list snapshot tags in a rig |
| [`save-snapshot restore`](#save-snapshot-restore) | copy a snapshot back into <rig>/saves/ (never overwrites without --replace) |
| [`scenario`](#scenario) | named test scenarios with expected results (plan is a dry run; check reads a log/save) |
| [`scenario list`](#scenario-list) | list bundled scenarios |
| [`scenario plan`](#scenario-plan) | show the mods, probe-config command and snapshot a scenario needs (writes nothing) |
| [`scenario check`](#scenario-check) | evaluate a scenario's expected results against a probe log (and optionally a save) |
| [`archaeology`](#archaeology) | build a deterministic static architecture and cross-reference map (read-only) |
| [`behavior-map`](#behavior-map) | derive the D1 behavior model, risks, hypotheses and unknowns from archaeology |
| [`risk-register`](#risk-register) | derive the D1 risk register and companion behavior artifacts from archaeology |
| [`hypotheses`](#hypotheses) | derive D1 artifacts, or synthesize D3 tests with --tests |
| [`probe-baseline`](#probe-baseline) | import captured observations as a no-verdict D2 baseline |
| [`save-baseline`](#save-baseline) | convert a historical Starsector save into a no-verdict D2 baseline (read-only input) |
| [`expect`](#expect) | validate D4/D5 expected-change decisions |
| [`expect check`](#expect-check) | validate expected-changes.json and its breadcrumbs |
| [`expect add`](#expect-add) | add a PROPOSED expected change beside a mod's reports |
| [`expect approve`](#expect-approve) | approve an expected-change decision |
| [`expect retire`](#expect-retire) | retire an expected-change decision |
| [`behavior-diff`](#behavior-diff) | compare two D2 baselines and classify every observation delta |
| [`release-behavior-evaluate`](#release-behavior-evaluate) | evaluate the D5/D6 behavior release gate |
| [`coverage`](#coverage) | build the D6 counts-only behavior coverage matrix |
| [`behavior-decide`](#behavior-decide) | apply written decisions to D1 risks/unknowns -> risks.decided.json / unknowns.decided.json for the release gate |
| [`docs-index`](#docs-index) | regenerate docs/CHECKS.md (every finding id: where it's emitted, tests, bug classes; scanner helpers) and docs/COMMANDS.md (every command and argument) from the source |
| [`lookup`](#lookup) | query a mod's archaeology graph for a class/id/file: definitions, references, related classes, lifecycle, save evidence, source/bytecode status, findings, runtime observations, and an inspection priority |
| [`novelty`](#novelty) | compare a mod's fingerprint (API usage, lifecycle patterns, structures, findings, weak ownership) with the mods already seen |
| [`dependency-substitutes`](#dependency-substitutes) | rank visible mods that could replace a missing or discontinued dependency (EXACT / PARTIAL / NONE) and recommend SWAP, REVIVE_DEPENDENCY, STRIP_FROM_MOD or ESCALATE |
| [`preset-check`](#preset-check) | check bf-test.ps1 presets against the rig's installed mods: own mod and declared dependencies enabled, enabled ids installed, no undeclared libraries |
| [`compile-check`](#compile-check) | compile a mod's loose scripts against RC8 with javac and report every error (javac, not the game's runtime Janino compiler) |
| [`diff-data`](#diff-data) | compare two Starsector data files (JSON dialect or CSV) by value: key/row/column order, comments and formatting never count; exit 1 when they differ |
| [`rebuild-from-reference`](#rebuild-from-reference) | port a mod's edited copies of vanilla files (.ship/.wpn/.variant/.skin/.system) to RC8: three-way merge of the reference install's vanilla file, the mod's copy and RC8's file; RC8's changes kept, only the mod's edits applied, overlaps reported as conflicts |
| [`corpus-index`](#corpus-index) | index a large mod archive once (inside .zip files too) and search it like grep in milliseconds; reports what it could not read so "no hits" means something |
| [`corpus-index build`](#corpus-index-build) | index (or refresh) every file under ROOT; unchanged files are skipped on re-runs |
| [`corpus-index search`](#corpus-index-search) | find files whose content (3+ characters, case-insensitive) or path contains TEXT |
| [`api-diff`](#api-diff) | compare two game API jars (e.g. an old starfarer.api.jar and RC8's): every public class, method and field removed or changed, with same-name candidates for where it went |
| [`revenantlib-check`](#revenantlib-check) | check a RevenantLib jar provides every bf.* method BridgeForge's fixers rewrite calls to, and (given the mod folder) that no source file lacks a compiled class |
| [`rebuild-jar`](#rebuild-jar) | rebuild a mod's jar from its sources and compare it with the original: class/method/field added or removed, forbidden sandbox references |
| [`fold`](#fold) | fold a discontinued library-like mod into a RevenantLib-shaped target directory (roadmap P14 item 10): copy it byte-for-byte, keeping its ids and class names, and record provenance plus a dependency_successors.json entry |

## scan

scan a mod directory without modifying it

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `--output` OUTPUT | default bridgeforge-artifacts | output FOLDER (created if missing) for MODERNIZATION_REPORT.md and bridgeforge.compat.json; not a file name |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--vanilla-core` VANILLA_CORE | - | path to a read-only starsector-core directory, used for vanilla-row/faction/path exemptions |
| `--baseline` BASELINE | - | only report findings not present in this baseline file, plus a count of previously accepted findings that are now resolved |
| `--write-baseline` WRITE_BASELINE | - | write the current scan's finding keys to this file as an accepted baseline |
| `--compile-check` | - | also javac-compile loose scripts against RC8 (needs --vanilla-core); opt-in, off by default so scans stay fast and hermetic |

## bytecode-inspect

inspect class/JAR symbolic references without rewriting

| Argument | Notes | Help |
|---|---|---|
| `input` | nargs + | - |
| `--output` OUTPUT | - | - |

## bytecode-diff

compare class/JAR symbolic and structural inventories

| Argument | Notes | Help |
|---|---|---|
| `before` | nargs + | - |
| `--after` AFTER | required; nargs + | - |
| `--output` OUTPUT | - | - |

## bytecode-plan

produce review-only exact bytecode remap candidates

| Argument | Notes | Help |
|---|---|---|
| `input` | nargs + | - |
| `--rules` RULES | required | - |
| `--output` OUTPUT | - | - |

## bytecode-apply

apply exact approved bytecode remaps to an output .class copy

| Argument | Notes | Help |
|---|---|---|
| `input` | - | - |
| `--rules` RULES | required | - |
| `--approve` APPROVE | required; repeatable | - |
| `--output` OUTPUT | required | - |

## workspace

create an immutable-reference workspace and working copy

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `--output` OUTPUT | required | - |

## plan

produce an explicit migration plan for a workspace

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--rules` PACK_JSON | repeatable | additional migration-rule pack (repeatable) |
| `--pack` PACK_ID | repeatable | bundled migration pack to use (repeatable) |

## apply

apply only explicitly approved planned rules

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |
| `--approve` RULE_ID | repeatable | - |
| `--safe` | - | apply all SAFE planned rules; REVIEW rules still need --approve |

## rollback

restore a working copy from a checkpoint

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |
| `checkpoint` | - | - |

## build-plan

model a compile environment without running javac

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--jdk` JDK | - | - |
| `--api-jar` API_JAR | repeatable | - |
| `--starsector-install` STARSECTOR_INSTALL | - | explicit local Starsector install; hash-record its core compile classpath |
| `--dependency-jar` DEPENDENCY_JAR | repeatable | - |
| `--library-registry` LIBRARY_REGISTRY | - | local dependency-id -> jar-path map (never bundled or committed) |

## compile

run the explicit workspace build profile

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |

## package-jar

package successful workspace classes into a reviewable JAR output copy

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |
| `input_jar` | - | - |
| `--output-name` OUTPUT_NAME | - | - |

## compile-feedback

link compiler evidence to planned migration candidates

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |

## review-bundle

create a bounded human/agent review handoff

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |

## validate

run workspace-integrity and structural validation

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |

## save-risk

flag changed persistent-identifier-shaped fields

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |

## pipeline

run the auditable Bridgeforge modernization pipeline

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--rules` RULES | repeatable | - |
| `--pack` PACK | repeatable | - |
| `--approve` APPROVE | repeatable | - |
| `--safe` | - | - |
| `--jdk` JDK | - | - |
| `--api-jar` API_JAR | repeatable | - |
| `--starsector-install` STARSECTOR_INSTALL | - | explicit local Starsector install; hash-record its core compile classpath |
| `--dependency-jar` DEPENDENCY_JAR | repeatable | - |
| `--library-registry` LIBRARY_REGISTRY | - | local dependency-id -> jar-path map (never bundled or committed) |
| `--compile` | - | - |
| `--bytecode-file` BYTECODE_FILE | - | - |
| `--bytecode-rules` BYTECODE_RULES | - | - |
| `--bytecode-approve` BYTECODE_APPROVE | repeatable | - |

## packs

list bundled migration packs

| Argument | Notes | Help |
|---|---|---|
| `--root` ROOT | - | - |

## migration-pack-candidate

create a non-applying evidence contract for a proposed library mapping

| Argument | Notes | Help |
|---|---|---|
| `--library-id` LIBRARY_ID | required | - |
| `--mapping-id` MAPPING_ID | required | - |
| `--from-symbol` FROM_SYMBOL | required | - |
| `--to-symbol` TO_SYMBOL | required | - |
| `--output` OUTPUT | required | - |

## runtime-profile

record an explicit opt-in runtime launch profile

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |
| `--executable` EXECUTABLE | required | - |
| `--argument` ARGUMENT | repeatable | - |
| `--working-directory` WORKING_DIRECTORY | required | - |
| `--timeout` TIMEOUT | default 60 | - |
| `--log-file` LOG_FILE | - | - |
| `--expect-log` EXPECT_LOG | repeatable | - |
| `--scenario` SCENARIO | repeatable; one of campaign-load, mission-launch, custom-ui | - |
| `--scenario-expect` SCENARIO=LOG_MARKER | repeatable | require a log marker for one selected scenario; repeatable |

## runtime-smoke

inspect or explicitly run a runtime profile

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |
| `--execute` | - | - |

## inspect

show workspace plans, diffs, and checkpoints

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |

## export-patch

export a patch-only package

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |
| `--output` OUTPUT | required | - |

## opportunities

report non-applying library-adoption opportunities

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |

## doctor

check local tooling, packs, and optional workspace integrity

| Argument | Notes | Help |
|---|---|---|
| `--workspace` WORKSPACE | - | - |
| `--json` | - | - |

## conflicts

detect planned-edit and duplicate-class conflicts

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |

## provenance

write deterministic workspace and artifact hashes

| Argument | Notes | Help |
|---|---|---|
| `workspace` | - | - |

## corpus-compare

compare an explicitly selected local mod to a sanitized baseline

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `--baseline` BASELINE | required | - |

## corpus-audit

write a read-only aggregate report for explicit mod directories

| Argument | Notes | Help |
|---|---|---|
| `mod_directories` | nargs + | - |
| `--output` OUTPUT | required | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--continue-on-error` | - | - |
| `--max-files-per-mod` MAX_FILES_PER_MOD | - | - |
| `--max-jars-per-mod` MAX_JARS_PER_MOD | - | - |

## cross-mod-analyze

build a read-only dependency, class, and campaign-ID graph for explicit mod directories

| Argument | Notes | Help |
|---|---|---|
| `mod_directories` | nargs + | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--output` OUTPUT | - | - |
| `--alias` DIRECTORY_NAME=MOD_ID | repeatable | explicit identity for a selected non-standard metadata layout; repeatable |

## campaign-identity-inventory

inventory source-defined campaign IDs from explicit mod directories

| Argument | Notes | Help |
|---|---|---|
| `mod_directories` | nargs + | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--output` OUTPUT | required | - |

## campaign-identity-check

resolve campaign literal lookups against an explicit identity inventory

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `--inventory` INVENTORY | required | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--output` OUTPUT | - | - |

## decompiler-review

record or explicitly run a user-supplied decompiler as untrusted review evidence

| Argument | Notes | Help |
|---|---|---|
| `input` | - | - |
| `--adapter` ADAPTER | required | - |
| `--adapter-argument` ADAPTER_ARGUMENT | repeatable | adapter argument; include {input} and {output} placeholders |
| `--output` OUTPUT | required | - |
| `--execute` | - | - |

## release-lineage

compare an explicitly ordered sequence of mod releases without modifying them

| Argument | Notes | Help |
|---|---|---|
| `release_directories` | nargs + | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--output` OUTPUT | - | - |

## archive-preflight

inspect a ZIP archive without extracting it

| Argument | Notes | Help |
|---|---|---|
| `archive` | - | - |
| `--output` OUTPUT | - | - |

## archive-stage

extract a preflight-safe ZIP into a new explicit destination

| Argument | Notes | Help |
|---|---|---|
| `archive` | - | - |
| `--output` OUTPUT | required | - |
| `--select-root` SELECT_ROOT | - | one preflight candidate mod root when the archive contains multiple |
| `--manifest-output` MANIFEST_OUTPUT | - | optional manifest path outside the staged directory |

## library-api-inventory

inventory class symbols in a supplied local library JAR

| Argument | Notes | Help |
|---|---|---|
| `jar` | - | - |
| `--output` OUTPUT | - | - |
| `--library-id` LIBRARY_ID | - | - |
| `--library-version` LIBRARY_VERSION | - | - |

## library-api-match

compare a mod's imports with a supplied local library API inventory

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `inventory` | - | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--output` OUTPUT | - | - |

## dependency-api-check

compare direct optional-mod imports with explicit local API inventories

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `--inventory` INVENTORY | required; repeatable | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--output` OUTPUT | - | - |

## release-evaluate

compare two release directories without modifying either

| Argument | Notes | Help |
|---|---|---|
| `before_directory` | - | - |
| `after_directory` | - | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--output` OUTPUT | - | - |

## revival-audit

audit final revival evidence and optionally attest its release ZIP

| Argument | Notes | Help |
|---|---|---|
| `candidate` | - | - |
| `--archive` ARCHIVE | - | - |
| `--output` OUTPUT | - | - |

## working-tree-layout

classify generated, backup, and source/content candidates without changing a mod

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `--output` OUTPUT | - | - |

## source-authority

record an explicit selected source root for a working tree

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `--select-root` SELECT_ROOT | required | - |
| `--output` OUTPUT | required | - |

## build-input-manifest

record source authority and annotation-processing build evidence

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `--source-root` SOURCE_ROOT | - | - |
| `--output` OUTPUT | required | - |

## integration-scenarios

suggest review-only runtime scenarios from optional integration evidence

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `--target-starsector` TARGET_STARSECTOR | default 0.98.x | - |
| `--target-java` TARGET_JAVA | default 17 | - |
| `--output` OUTPUT | - | - |

## log-triage

classify a Starsector log into FATAL / MOD-ERROR / KNOWN-NOISE / OTHER without modifying it

| Argument | Notes | Help |
|---|---|---|
| `log` | - | - |
| `--mod-prefix` PREFIX | repeatable | additional mod source/package prefix to attribute errors to; repeatable |
| `--mods-dir` MODS_DIR | - | mods folder the log ran with: names the mod whose jar owns each crash frame (suspect/involved) |
| `--all-mods` | - | with --mods-dir: index every mod folder, not just enabled_mods.json (e.g. a log from a different mod list) |
| `--json` | - | - |

## copy-drift

hash-compare a mod working copy against its deployed/test-rig copy

| Argument | Notes | Help |
|---|---|---|
| `working_copy` | - | - |
| `deployed_copy` | - | - |
| `--json` | - | - |

## jar-audit

compare a rebuilt mod jar with its original for bundled libraries, removed classes, and reflection use

| Argument | Notes | Help |
|---|---|---|
| `rebuilt` | - | - |
| `--original` ORIGINAL | required | original .jar file, or a .zip archive containing one |
| `--json` | - | - |

## build-tag

iterate a visible build tag in a revived mod's mod_info.json name/version, surgically (never re-serializes the file)

| Argument | Notes | Help |
|---|---|---|
| `mod_dir` | - | - |
| `--label` LABEL | default BF | suffix label used in the name tag, e.g. '[LABEL rN]' (default: BF) |
| `--set` SET_VALUE | - | force the build number instead of incrementing |
| `--dry-run` | - | print the proposed change without writing mod_info.json |
| `--no-manifest` | - | don't record the per-tag file hash manifest that test-plan diffs against |
| `--record-only` | - | record the manifest for the CURRENT build (r0 if untagged) without bumping or writing mod_info.json |
| `--manifests-dir` MANIFESTS_DIR | - | where build manifests go (default: <repo>/bridgeforge-state/build-manifests; never inside the mod) |
| `--json` | - | - |

## translate-export

export every non-English (CJK) player-visible string with a stable id (read-only; handles Starsector's lenient CSV/JSON and jar strings)

| Argument | Notes | Help |
|---|---|---|
| `mod_dir` | - | - |
| `output` | - | translation JSON to write (must not be inside the mod) |
| `--record` RECORD | repeatable | zh/en record JSON file or folder to prefill from (e.g. a translator's ai/en); repeatable |
| `--reference` REFERENCE | - | an English copy of the same mod: prefill CSV text by row+column and JSON text by path |
| `--json` | - | - |

## translate-apply

write a filled translation JSON into a copy of the mod (or an explicit working copy with --in-place)

| Argument | Notes | Help |
|---|---|---|
| `mod_dir` | - | - |
| `translations` | - | - |
| `--out` OUT | - | new folder for the translated copy |
| `--in-place` | - | edit mod_dir itself (use only on a working copy) |
| `--json` | - | - |

## translate-tm

write a translation JSON's pairs as Project Go (SSMT) translation memory, for `ssmt tm import <db> json <file>`

| Argument | Notes | Help |
|---|---|---|
| `translations` | - | - |
| `output` | - | - |
| `--context` CONTEXT | default  | Project Go TM context (default empty: exact matches ignore context; fuzzy matches filter on it) |
| `--json` | - | - |

## translate-check

report leftover non-English strings and designTypeColors keys that no tech/manufacturer uses

| Argument | Notes | Help |
|---|---|---|
| `mod_dir` | - | - |
| `--json` | - | - |

## fix

apply a SAFE, mechanical fixer for one supported finding id (dry-run diff by default)

| Argument | Notes | Help |
|---|---|---|
| `mod_dir` | - | - |
| `--finding` ID | required | supported: mod-info-game-version-inexact, csv-row-extra-columns, csv-missing-design-type-column, procgen-planet-row-missing, procgen-star-row-missing, faction-known-lists-missing, mod-info-triage-banner, wing-data-missing-role-desc-column, target-interface-method-missing, removed-api-call, carrier-bays-proposal, revenantlib-fold-conflict, undeclared-library-dependency |
| `--apply` | - | write the change (default: print a dry-run diff only) |
| `--json` | - | - |
| `--target-game-version` TARGET_GAME_VERSION | - | required for mod-info-game-version-inexact |
| `--design-type` DESIGN_TYPE | - | required for csv-missing-design-type-column |
| `--id-prefix` PREFIX | repeatable | repeatable; required for csv-missing-design-type-column |
| `--design-color` R,G,B | - | required for csv-missing-design-type-column |
| `--vanilla-core` VANILLA_CORE | - | required for procgen-*-row-missing and faction-known-lists-missing |
| `--type-id` TYPE_ID | - | required for procgen-planet-row-missing/procgen-star-row-missing |
| `--from-vanilla-id` FROM_VANILLA_ID | - | required for procgen-planet-row-missing/procgen-star-row-missing |
| `--faction-file` FACTION_FILE | - | required for faction-known-lists-missing |
| `--hull` ID=N | repeatable | repeatable; required for carrier-bays-proposal, e.g. --hull my_carrier=4 (0-6, vanilla's own maximum) |
| `--allow-shadowed-edit` | - | override the refusal to edit a loose script one of this mod's own jars already shadows (the edit has no effect unless the jar is also being rebuilt from the patched source) |

## prepare-test

sync a rig test copy from a working copy, and optionally run a boot test

| Argument | Notes | Help |
|---|---|---|
| `working_dir` | - | - |
| `rig_mod_dir` | - | - |
| `--sync` | - | copy drifted/missing working -> rig files (never the reverse, never deletes rig extras) |
| `--bump` | - | apply_build_tag the working copy before comparing |
| `--label` LABEL | default BF | - |
| `--no-manifest` | - | with --bump: don't record the per-tag build manifest for test-plan |
| `--boot` RUNTIME_DIR | - | run a boot test against this runtime after a successful sync |
| `--mods` ID | nargs + | mod ids to enable for --boot |
| `--timeout` TIMEOUT | default 240 | - |
| `--log-name` LOG_NAME | - | - |
| `--keep-mods` | - | - |
| `--json` | - | - |

## boot-test

run a boot smoke test of a Starsector runtime with explicit mods enabled

| Argument | Notes | Help |
|---|---|---|
| `runtime_dir` | - | - |
| `--mods` ID | required; nargs + | - |
| `--pack` SET | - | also run a compat-set matrix (roadmap P4): --mods alone with the libs it needs, then --mods plus this whole named set (e.g. 'standard') |
| `--timeout` TIMEOUT | default 240 | - |
| `--log-name` LOG_NAME | - | - |
| `--keep-mods` | - | - |
| `--json` | - | - |

## compat-set

install a named standard compatibility pack (roadmap P4) into a test rig

No arguments.

## compat-set install

copy a named compat set's mods from a source mods directory into a rig's mods/

| Argument | Notes | Help |
|---|---|---|
| `SET` | - | - |
| `--runtime` RUNTIME | required | isolated test-rig runtime directory (starsector-core must be a junction/symlink) |
| `--source-mods` SOURCE_MODS | required | read-only directory to copy mods FROM (never written to) |
| `--reference-manifest` REFERENCE_MANIFEST | - | allow a registered dedicated historical install whose core is not a junction |
| `--dry-run` | - | report the install plan without copying anything |
| `--json` | - | - |

## dossier

build a size-capped agent triage dossier for a mod (index + parts)

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `--vanilla-core` VANILLA_CORE | - | - |
| `--baseline` BASELINE | - | only include findings not present in this scan baseline |
| `--original-jar` ORIGINAL_JAR | - | original .jar (or .zip containing one) for the jar-audit artifact |
| `--rig` RIG | - | deployed/test-rig mod copy for the copy-drift artifact |
| `--log` LOG | - | Starsector stdout log for the log-triage artifact |
| `--save` SAVE | - | rig save dir or campaign.xml for a runtime-footprint section (live classes, scripts, factions, markets) |
| `--perf` PERF | - | SPW performance-report.json for a performance section |
| `--perf-mod-prefix` PREFIX | repeatable | log-spam prefix for the performance section (needs --log); repeatable |
| `--discovery-dir` DISCOVERY_DIR | - | directory containing D0-D6 JSON artifacts for dossier presentation |
| `--output` OUTPUT | - | output directory (default: bridgeforge-dossier/<mod-id> under the cwd); never inside In operation/Done |
| `--max-kb` MAX_KB | default 40.0 | per-file (index and each part) size cap in KB (default: 40.0) |
| `--context-lines` CONTEXT_LINES | default 5 | snippet context lines around a flagged line (default: 5) |
| `--json` | - | - |

## build-probe-mod

compile the bridgeforge-probe in-game probe mod (roadmap P3) and pack it deterministically

| Argument | Notes | Help |
|---|---|---|
| `--jdk` JDK | required | JDK 17+ home directory (containing bin/javac.exe) |
| `--core` CORE | required | starsector-core directory (for starfarer.api.jar and its sibling jars) |
| `--install-release` | - | also assemble the runtime-only release copy under probe-mod/releases/ |
| `--json` | - | - |

## probe-config

write bf_probe_config/bf_probe_rig into a test rig's saves/common, and optionally install the probe mod

| Argument | Notes | Help |
|---|---|---|
| `mod_dir` | - | - |
| `--runtime` RUNTIME | required | isolated test-rig runtime directory (starsector-core must be a junction/symlink) |
| `--seconds` SECONDS | default 60.0 | combat probe duration in seconds (default: 60) |
| `--campaign-interval-days` CAMPAIGN_INTERVAL_DAYS | default 5.0 | campaign probe re-run interval in in-game days (default: 5) |
| `--combat-cap` COMBAT_CAP | default 12 | max hulls per side in the combat probe mission (default: 12) |
| `--track` ENTITY | repeatable | custom entity id to track position for; repeatable |
| `--setup` SPEC | repeatable | in-game setup applied once per save via the public API: rep:<faction>=<RepLevel\|n>, credits:<N>, ship:<variant>[:<count>], spawn-fleet:<faction>:<fp>, jump:<system>; repeatable |
| `--profile` NAME\|PATH | - | setup profile (Notepad-friendly .txt): a bundled name (exigency-rep, seeker-betelgeuse, flux-basic) or a path; merged after --setup |
| `--install` | - | also install/refresh the probe mod into <runtime>/mods/bridgeforge-probe/ from the release copy |
| `--dry-run` | - | report what would be written without touching the rig |
| `--json` | - | - |

## save-compat

check whether a save's referenced mod classes exist in the build's loaded jars (read-only)

| Argument | Notes | Help |
|---|---|---|
| `save` | - | save directory or its campaign.xml |
| `mod_dir` | - | - |
| `--vanilla-core` VANILLA_CORE | - | starsector-core directory, to help rule out vanilla-only aliases |
| `--compare-old` COMPARE_OLD | - | compare builds instead: old jar or mod dir (requires --compare-new) |
| `--compare-new` COMPARE_NEW | - | compare builds: new jar or mod dir (requires --compare-old) |
| `--json` | - | - |

## save-inspect

stream a save and report tracked mod state, known-list sizes and per-mod object counts (read-only)

| Argument | Notes | Help |
|---|---|---|
| `save` | - | save directory or its campaign.xml |
| `--mod-dir` MOD_DIR | - | - |
| `--json` | - | - |
| `--track-class` ALIAS | repeatable | class alias to report scalar fields for; repeatable |
| `--track-id` ID | repeatable | entity id to locate; repeatable |
| `--vanilla-core` VANILLA_CORE | - | - |

## save-diff

compare tracked mod state between two saves, e.g. campaign.xml vs campaign.xml.bak (read-only)

| Argument | Notes | Help |
|---|---|---|
| `save` | - | save directory or its campaign.xml |
| `save_b` | - | second save (or campaign.xml.bak) to compare against |
| `--mod-dir` MOD_DIR | - | - |
| `--json` | - | - |
| `--track-class` ALIAS | repeatable | class alias to report scalar fields for; repeatable |
| `--track-id` ID | repeatable | entity id to locate; repeatable |
| `--vanilla-core` VANILLA_CORE | - | - |

## save-scripts

count a mod's scripts/listeners per holder in a save and flag duplicates (read-only)

| Argument | Notes | Help |
|---|---|---|
| `save` | - | save directory or its campaign.xml |
| `mod_dir` | - | - |
| `--json` | - | - |
| `--vanilla-core` VANILLA_CORE | - | - |

## save-growth

per-mod object counts and file sizes across a save chain, with a growth warning (read-only)

| Argument | Notes | Help |
|---|---|---|
| `saves` | nargs + | save directories or campaign.xml files, oldest first |
| `--mod-dir` MOD_DIR | - | - |
| `--json` | - | - |
| `--warn-ratio` WARN_RATIO | default 2.0 | growth multiplier between consecutive saves that triggers a warning (default: 2.0) |

## save-provenance

compare the mod versions/BF build tags a save was made with against current working copies (read-only)

| Argument | Notes | Help |
|---|---|---|
| `save` | - | save directory or its campaign.xml |
| `--mod-dir` MOD_DIR | required; repeatable | mod working copy; repeatable |
| `--json` | - | - |

## save-content

check the data ids a save stores (hulls, variants, weapons, factions, ...) against the build plus vanilla (read-only)

| Argument | Notes | Help |
|---|---|---|
| `save` | - | save directory or its campaign.xml |
| `mod_dir` | - | - |
| `--json` | - | - |
| `--vanilla-core` VANILLA_CORE | - | - |

## save-removal

INTERNAL ONLY: everything a save still depends on for one mod; SAFE_TO_REMOVE or UNSAFE (read-only)

| Argument | Notes | Help |
|---|---|---|
| `save` | - | save directory or its campaign.xml |
| `mod_dir` | - | - |
| `--json` | - | - |
| `--vanilla-core` VANILLA_CORE | - | - |

## save-summary

write a redacted, shareable save summary with no player data (local file only)

| Argument | Notes | Help |
|---|---|---|
| `save` | - | save directory or its campaign.xml |
| `--mod-dir` MOD_DIR | required; repeatable | mod working copy; repeatable |
| `--json` | - | - |
| `--out` OUT | required | output JSON path |
| `--vanilla-core` VANILLA_CORE | - | - |

## bootstrap

one-time: write a scan baseline and record the current build manifest for each mod (nothing written inside mods)

| Argument | Notes | Help |
|---|---|---|
| `mod_dirs` | nargs + | - |
| `--vanilla-core` VANILLA_CORE | required | - |
| `--baselines-dir` BASELINES_DIR | - | default: <repo>/bridgeforge-state/baselines |
| `--manifests-dir` MANIFESTS_DIR | - | default: <repo>/bridgeforge-state/build-manifests |
| `--overwrite` | - | replace existing baselines (default: keep them) |
| `--json` | - | - |

## who-locks

read-only process/Restart Manager diagnosis; never stops processes

| Argument | Notes | Help |
|---|---|---|
| `path` | - | - |
| `--json` | - | - |

## intake

preserve a ZIP and create a fresh convention-layout assessment copy

| Argument | Notes | Help |
|---|---|---|
| `archive` | - | - |
| `--repo-root` REPO_ROOT | default current directory | - |
| `--name` NAME | - | portable mod folder name (default: sanitized metadata id) |
| `--selected-root` SELECTED_ROOT | - | exact ZIP mod-root candidate when ambiguous |
| `--archaeology` | - | also collect static D0 discovery evidence |
| `--json` | - | - |

## board

read-only declared-evidence status board; missing evidence remains unknown

| Argument | Notes | Help |
|---|---|---|
| `--repo-root` REPO_ROOT | default current directory | - |
| `--write` | - | write STATUS.generated.json/.md; never replaces manual STATUS.md |
| `--json` | - | - |

## promote

stage/audit a release and retain prior builds; dry-run by default

| Argument | Notes | Help |
|---|---|---|
| `mod` | - | folder name under In operation (not a source path) |
| `--repo-root` REPO_ROOT | default current directory | - |
| `--original` ORIGINAL | required | - |
| `--baseline` BASELINE | required | - |
| `--behavior-diff` BEHAVIOR_DIFF | required | - |
| `--behavior-risks` BEHAVIOR_RISKS | required | - |
| `--behavior-unknowns` BEHAVIOR_UNKNOWNS | required | - |
| `--expected-changes` EXPECTED_CHANGES | - | - |
| `--vanilla-core` VANILLA_CORE | - | - |
| `--rig` RIG | - | - |
| `--corpus-dir` CORPUS_DIR | - | - |
| `--policy` POLICY | - | - |
| `--apply` | - | - |
| `--json` | - | - |

## rig-doctor

pre-flight checks for a test rig: isolation, running game, probe install, enabled mods, working-copy drift, real-install saves untouched

| Argument | Notes | Help |
|---|---|---|
| `runtime_dir` | - | - |
| `--working` ID=PATH | repeatable | working copy for a mod id (adds to/overrides the defaults); repeatable |
| `--no-default-working` | - | don't use the project's known working-copy mapping |
| `--real-install` REAL_INSTALL | - | real Starsector install, to confirm its saves are untouched (read-only) |
| `--write-saves-baseline` | - | record the real install's saves listing as the baseline (the only write) |
| `--reference-manifest` REFERENCE_MANIFEST | - | P10 historical rig manifest; verifies the dedicated install and skips the incompatible RC8 probe check |
| `--json` | - | - |

## rig-create

register a dedicated historical Starsector install as a P10 reference rig

| Argument | Notes | Help |
|---|---|---|
| `--game-version` GAME_VERSION | required | - |
| `--install` INSTALL | required | dedicated old-game install; never select the player's primary install |
| `--output` OUTPUT | - | manifest path (default: bridgeforge-state/reference-rigs/<version>.json) |
| `--replace` | - | replace an existing manifest after re-inventorying the selected install |
| `--json` | - | - |

## test-plan

list the minimal live tests and probe assertions for what changed since a build tag (read-only)

| Argument | Notes | Help |
|---|---|---|
| `mod_dir` | - | - |
| `--since` TAG | required | build tag to diff against, e.g. r3 or 3 |
| `--manifests-dir` MANIFESTS_DIR | - | - |
| `--live-test-instructions` LIVE_TEST_INSTRUCTIONS | - | default: In operation/LIVE_TEST_INSTRUCTIONS.md |
| `--json` | - | - |

## perf-gate

summarise an SPW performance report and log spam; PASS/WARN/FAIL against thresholds (report-only by default)

| Argument | Notes | Help |
|---|---|---|
| `--perf-report` PERF_REPORT | - | SPW performance-report.json |
| `--log` LOG | - | Starsector stdout log for spam counts |
| `--mod-prefix` PREFIX | repeatable | mod id/package for log-spam counting; repeatable |
| `--min-repeats` MIN_REPEATS | default 5 | - |
| `--thresholds` THRESHOLDS | - | JSON {"metric": {"warn": x, "fail": y}}; omit for report-only |
| `--json` | - | - |

## release

run the release gates and, with --apply and all gates passing, package the mod (dry run by default)

| Argument | Notes | Help |
|---|---|---|
| `mod_dir` | - | - |
| `--original` ORIGINAL | required | original jar or archive for jar-audit |
| `--baseline` BASELINE | required | reviewed scan baseline; any new MANUAL finding blocks |
| `--out-dir` OUT_DIR | required | - |
| `--vanilla-core` VANILLA_CORE | - | - |
| `--rig` RIG | - | rig copy of the mod for the copy-drift gate |
| `--corpus-dir` CORPUS_DIR | - | save corpus to re-check (default: In operation/save_corpus/<mod-id>/) |
| `--policy` POLICY | - | licence policy JSON (default: bundled release_policy.json) |
| `--behavior-diff` BEHAVIOR_DIFF | - | D5 behavior-diff JSON; required by the D-series release gate |
| `--behavior-risks` BEHAVIOR_RISKS | - | D1 risks.json consumed by the behavior release gate |
| `--behavior-unknowns` BEHAVIOR_UNKNOWNS | - | D1 unknowns.json consumed by the behavior release gate |
| `--expected-changes` EXPECTED_CHANGES | - | D4 expected-changes.json used for validation and release notes |
| `--apply` | - | write the release (only if every gate passes) |
| `--json` | - | - |

## save-snapshot

tag, list and restore rig-only save copies (never touches a real install)

No arguments.

## save-snapshot tag

copy <rig>/saves/<save> into <rig>/save_snapshots/<tag>/ with a hash manifest

| Argument | Notes | Help |
|---|---|---|
| `runtime_dir` | - | - |
| `save_name` | - | - |
| `tag` | - | - |
| `--json` | - | - |

## save-snapshot list

list snapshot tags in a rig

| Argument | Notes | Help |
|---|---|---|
| `runtime_dir` | - | - |
| `--json` | - | - |

## save-snapshot restore

copy a snapshot back into <rig>/saves/ (never overwrites without --replace)

| Argument | Notes | Help |
|---|---|---|
| `runtime_dir` | - | - |
| `tag` | - | - |
| `--as-name` AS_NAME | - | restore under this save folder name instead of the original |
| `--replace` | - | overwrite an existing save folder of the same name |
| `--json` | - | - |

## scenario

named test scenarios with expected results (plan is a dry run; check reads a log/save)

No arguments.

## scenario list

list bundled scenarios

| Argument | Notes | Help |
|---|---|---|
| `--json` | - | - |

## scenario plan

show the mods, probe-config command and snapshot a scenario needs (writes nothing)

| Argument | Notes | Help |
|---|---|---|
| `name` | - | - |
| `runtime_dir` | - | - |
| `--mod` ID=PATH | repeatable | source directory for a mod id the scenario uses; repeatable |
| `--json` | - | - |

## scenario check

evaluate a scenario's expected results against a probe log (and optionally a save)

| Argument | Notes | Help |
|---|---|---|
| `name` | - | - |
| `log_path` | - | - |
| `--save` SAVE | - | save directory or campaign.xml for save assertions |
| `--json` | - | - |

## archaeology

build a deterministic static architecture and cross-reference map (read-only)

| Argument | Notes | Help |
|---|---|---|
| `mod_directory` | - | - |
| `--output` OUTPUT | required | - |
| `--save-aliases` SAVE_ALIASES | - | optional real-save alias evidence for confirmed persistent classes |
| `--vanilla-core` VANILLA_CORE | - | read-only starsector-core, so assets a mod borrows from vanilla are not reported as missing |
| `--json` | - | - |

## behavior-map

derive the D1 behavior model, risks, hypotheses and unknowns from archaeology

| Argument | Notes | Help |
|---|---|---|
| `architecture` | - | - |
| `--output` OUTPUT | required | - |
| `--json` | - | - |

## risk-register

derive the D1 risk register and companion behavior artifacts from archaeology

| Argument | Notes | Help |
|---|---|---|
| `architecture` | - | - |
| `--output` OUTPUT | required | - |
| `--json` | - | - |

## hypotheses

derive D1 artifacts, or synthesize D3 tests with --tests

| Argument | Notes | Help |
|---|---|---|
| `input` | - | architecture.json, or hypotheses.json with --tests |
| `--output` OUTPUT | required | - |
| `--tests` | - | synthesize proposed adversarial tests from hypotheses.json |
| `--json` | - | - |

## probe-baseline

import captured observations as a no-verdict D2 baseline

| Argument | Notes | Help |
|---|---|---|
| `input` | - | - |
| `--build` BUILD | required | - |
| `--scenario` SCENARIO | required | - |
| `--reference-kind` REFERENCE_KIND | one of old-game-rig, build, partial-original, static-floor; default build | - |
| `--output` OUTPUT | required | - |
| `--json` | - | - |

## save-baseline

convert a historical Starsector save into a no-verdict D2 baseline (read-only input)

| Argument | Notes | Help |
|---|---|---|
| `save` | - | save directory or campaign.xml |
| `--build` BUILD | required | - |
| `--scenario` SCENARIO | required | - |
| `--track-class` TRACK_CLASS | repeatable | - |
| `--track-id` TRACK_ID | repeatable | - |
| `--mod-dir` MOD_DIR | - | - |
| `--output` OUTPUT | required | - |
| `--json` | - | - |

## expect

validate D4/D5 expected-change decisions

No arguments.

## expect check

validate expected-changes.json and its breadcrumbs

| Argument | Notes | Help |
|---|---|---|
| `expected` | - | - |
| `--artifact` ARTIFACT | repeatable | - |
| `--known-build` KNOWN_BUILD | repeatable | - |
| `--output` OUTPUT | - | - |
| `--json` | - | - |

## expect add

add a PROPOSED expected change beside a mod's reports

| Argument | Notes | Help |
|---|---|---|
| `expected` | - | - |
| `--mod-id` MOD_ID | required | - |
| `--id` CHANGE_ID | required | - |
| `--build` BUILD | required | - |
| `--layer` LAYER | required; one of runtime, save, static | - |
| `--summary` SUMMARY | required | - |
| `--why` WHY | required | - |
| `--observation` OBSERVATION | required | - |
| `--subject` SUBJECT | required | - |
| `--field` FIELD | - | - |
| `--change` CHANGE | required; one of from_to, count, added, removed, renamed, any | - |
| `--from` FROM_VALUE | - | - |
| `--to` TO_VALUE | - | - |
| `--old` OLD | - | - |
| `--new` NEW | - | - |
| `--link` KIND=ID | repeatable | risk, hyp, test or bug_class breadcrumb; repeatable |
| `--proposed-by` PROPOSED_BY | default agent | - |
| `--json` | - | - |

## expect approve

approve an expected-change decision

| Argument | Notes | Help |
|---|---|---|
| `expected` | - | - |
| `id` | - | - |
| `--by` BY | required | - |
| `--on` ON | required | - |
| `--json` | - | - |

## expect retire

retire an expected-change decision

| Argument | Notes | Help |
|---|---|---|
| `expected` | - | - |
| `id` | - | - |
| `--by` BY | required | - |
| `--on` ON | - | - |
| `--why` WHY | required | - |
| `--json` | - | - |

## behavior-diff

compare two D2 baselines and classify every observation delta

| Argument | Notes | Help |
|---|---|---|
| `before` | - | - |
| `after` | - | - |
| `--expected` EXPECTED | - | - |
| `--before-map` BEFORE_MAP | - | D0 architecture.json for the original/reference |
| `--after-map` AFTER_MAP | - | D0 architecture.json for the working/revived build |
| `--output` OUTPUT | required | - |
| `--json` | - | - |

## release-behavior-evaluate

evaluate the D5/D6 behavior release gate

| Argument | Notes | Help |
|---|---|---|
| `diff` | - | - |
| `--risks` RISKS | - | - |
| `--unknowns` UNKNOWNS | - | - |
| `--output` OUTPUT | - | - |
| `--json` | - | - |

## coverage

build the D6 counts-only behavior coverage matrix

| Argument | Notes | Help |
|---|---|---|
| `behavior` | - | - |
| `--baseline` BASELINE | repeatable | - |
| `--diff` DIFF | - | - |
| `--tests` TESTS | - | - |
| `--unknowns` UNKNOWNS | - | - |
| `--output` OUTPUT | required | - |
| `--json` | - | - |

## behavior-decide

apply written decisions to D1 risks/unknowns -> risks.decided.json / unknowns.decided.json for the release gate

| Argument | Notes | Help |
|---|---|---|
| `discovery_dir` | - | folder holding behavior.json, risks.json, unknowns.json |
| `decisions` | - | decisions JSON (schema_version 1; each decision: select, status, why, by, on[, evidence, applies_to]) |
| `--output` OUTPUT | - | where the .decided.json files go (default: the discovery folder) |
| `--json` | - | - |

## docs-index

regenerate docs/CHECKS.md (every finding id: where it's emitted, tests, bug classes; scanner helpers) and docs/COMMANDS.md (every command and argument) from the source

| Argument | Notes | Help |
|---|---|---|
| `--check` | - | don't write; exit 1 if either file is stale |
| `--json` | - | - |

## lookup

query a mod's archaeology graph for a class/id/file: definitions, references, related classes, lifecycle, save evidence, source/bytecode status, findings, runtime observations, and an inspection priority

| Argument | Notes | Help |
|---|---|---|
| `thing` | - | class name (full or short), id, file path or symbol; omit with --top for the treasure map |
| `--mod` MOD | required | mod working copy |
| `--discovery` DISCOVERY | - | discovery folder (default: <Mod>/reports/discovery/<working folder name>) |
| `--top` TOP | - | also rank the mod's classes and registrations by inspection priority |
| `--limit` LIMIT | default 3 | matches to show in detail (default 3) |
| `--vanilla-core` VANILLA_CORE | - | read-only starsector-core, used when the graph has to be rebuilt |
| `--json` | - | - |

## novelty

compare a mod's fingerprint (API usage, lifecycle patterns, structures, findings, weak ownership) with the mods already seen

| Argument | Notes | Help |
|---|---|---|
| `mod` | - | mod working copy |
| `--discovery` DISCOVERY | - | - |
| `--corpus` CORPUS | - | fingerprint folder (default: bridgeforge-state/corpus-fingerprints) |
| `--record` | - | add or refresh this mod's fingerprint in the corpus |
| `--vanilla-core` VANILLA_CORE | - | - |
| `--json` | - | - |

## dependency-substitutes

rank visible mods that could replace a missing or discontinued dependency (EXACT / PARTIAL / NONE) and recommend SWAP, REVIVE_DEPENDENCY, STRIP_FROM_MOD or ESCALATE

| Argument | Notes | Help |
|---|---|---|
| `mod` | - | mod working copy |
| `--providers` PROVIDERS | repeatable | mods folder, mod folder or In operation tree to search; repeatable (default: <repo>/In operation and its rig's mods) |
| `--vanilla-core` VANILLA_CORE | - | read-only starsector-core, so vanilla content isn't counted as missing |
| `--json` | - | - |

## preset-check

check bf-test.ps1 presets against the rig's installed mods: own mod and declared dependencies enabled, enabled ids installed, no undeclared libraries

| Argument | Notes | Help |
|---|---|---|
| `script` | - | path to bf-test.ps1 |
| `--rig-mods` RIG_MODS | - | rig mods folder (default: <script folder>/_rig/mods) |
| `--json` | - | - |

## compile-check

compile a mod's loose scripts against RC8 with javac and report every error (javac, not the game's runtime Janino compiler)

| Argument | Notes | Help |
|---|---|---|
| `mod` | - | mod working copy |
| `--vanilla-core` VANILLA_CORE | - | read-only starsector-core, put on the compile classpath |
| `--jdk` JDK | - | JDK home (default: this repo's rig JDK, else JAVA_HOME, else PATH) |
| `--providers` PROVIDERS | repeatable | mods folder, mod folder or In operation tree to search for declared dependencies; repeatable (default: <repo>/In operation and its rig's mods) |
| `--api-diff` API_DIFF | - | catalogue written by `api-diff`: attach removed/moved-API leads to matching javac errors |
| `--json` | - | - |

## diff-data

compare two Starsector data files (JSON dialect or CSV) by value: key/row/column order, comments and formatting never count; exit 1 when they differ

| Argument | Notes | Help |
|---|---|---|
| `a` | - | first file (e.g. the mod's copy) |
| `b` | - | second file (e.g. vanilla's or a reference install's) |
| `--json` | - | - |

## rebuild-from-reference

port a mod's edited copies of vanilla files (.ship/.wpn/.variant/.skin/.system) to RC8: three-way merge of the reference install's vanilla file, the mod's copy and RC8's file; RC8's changes kept, only the mod's edits applied, overlaps reported as conflicts

| Argument | Notes | Help |
|---|---|---|
| `mod` | - | mod folder (holding data/) |
| `--reference-core` REFERENCE_CORE | required | starsector-core of the install the mod was made for (read-only) |
| `--vanilla-core` VANILLA_CORE | required | RC8 starsector-core (read-only) |
| `--class` FILE_CLASSES | repeatable | limit to one file class, e.g. --class wpn; repeatable (default: all), so a large mod can be rebuilt in reviewable stages |
| `--output` OUTPUT | - | write MERGED files here, mirroring data/ paths (never into the mod or either core) |
| `--json` | - | - |

## corpus-index

index a large mod archive once (inside .zip files too) and search it like grep in milliseconds; reports what it could not read so "no hits" means something

No arguments.

## corpus-index build

index (or refresh) every file under ROOT; unchanged files are skipped on re-runs

| Argument | Notes | Help |
|---|---|---|
| `root` | - | folder to index, e.g. Downloads |
| `--db` DB | default bridgeforge-state/corpus-index.sqlite | index file (default: bridgeforge-state/corpus-index.sqlite) |
| `--max-bytes` MAX_BYTES | default 4194304 | skip (and report) text files larger than this |
| `--json` | - | - |

## corpus-index search

find files whose content (3+ characters, case-insensitive) or path contains TEXT

| Argument | Notes | Help |
|---|---|---|
| `text` | - | - |
| `--db` DB | default bridgeforge-state/corpus-index.sqlite | - |
| `--names` | - | match file paths only |
| `--limit` LIMIT | default 50 | - |
| `--json` | - | - |

## api-diff

compare two game API jars (e.g. an old starfarer.api.jar and RC8's): every public class, method and field removed or changed, with same-name candidates for where it went

| Argument | Notes | Help |
|---|---|---|
| `old` | - | older starfarer.api.jar, or the starsector-core folder holding it |
| `new` | - | newer starfarer.api.jar, or the starsector-core folder holding it |
| `--output` OUTPUT | - | write the JSON catalogue here (for compile-check --api-diff) |
| `--json` | - | - |

## revenantlib-check

check a RevenantLib jar provides every bf.* method BridgeForge's fixers rewrite calls to, and (given the mod folder) that no source file lacks a compiled class

| Argument | Notes | Help |
|---|---|---|
| `path` | - | RevenantLib.jar, the RevenantLib mod folder, or a repo root holding working/ |
| `--json` | - | - |

## rebuild-jar

rebuild a mod's jar from its sources and compare it with the original: class/method/field added or removed, forbidden sandbox references

| Argument | Notes | Help |
|---|---|---|
| `mod` | - | mod workspace (holding working/) or the working copy itself |
| `--sources` SOURCES | required | sources directory, relative to the workspace or the working copy (e.g. working/data/scripts) |
| `--jar` JAR | required | the ORIGINAL jar to rebuild and compare against, relative to the workspace, an original/<archive folder>/, or the working copy (e.g. jars/Name.jar) |
| `--vanilla-core` VANILLA_CORE | - | read-only starsector-core, put on the compile classpath |
| `--jdk` JDK | - | JDK home (default: this repo's rig JDK, else JAVA_HOME, else PATH) |
| `--providers` PROVIDERS | repeatable | mods folder, mod folder or In operation tree to search for declared dependencies; repeatable (default: <repo>/In operation and its rig's mods) |
| `--output` OUTPUT | - | where the rebuilt jar and compiled classes go (default: a new temp directory) |
| `--install` | - | only when status is PASS: move the working copy's current jar to scratch/moved-<date>/ (logged in MOVES.log) and copy the rebuilt jar in |
| `--json` | - | - |

## fold

fold a discontinued library-like mod into a RevenantLib-shaped target directory (roadmap P14 item 10): copy it byte-for-byte, keeping its ids and class names, and record provenance plus a dependency_successors.json entry

| Argument | Notes | Help |
|---|---|---|
| `source` | - | the library mod's own directory (holding mod_info.json), e.g. a working copy |
| `target` | - | the RevenantLib-shaped target directory (holds/gets original/<source name>/ and reports/PROVENANCE.md) |
| `--overwrite` | - | allow folding into an existing original/<source name>/ (files that differ from the source are still never overwritten -- reported as conflicts instead) |
| `--dry-run` | - | report what would happen; write nothing |
| `--successors-path` SUCCESSORS_PATH | - | dependency_successors.json to update (default: bridgeforge's own copy) |
| `--json` | - | - |
