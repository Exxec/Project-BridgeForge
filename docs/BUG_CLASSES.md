# Bug-class registry (roadmap P7)

One row per bug class actually found during live testing of the mods under `In operation/`
(2026-09-08 .. 2026-09-11, per `In operation/STATUS.md` and `CHANGELOG.md`). Per
`docs/REVIVAL_ASSURANCE_PLAN.md` principle 2 and the `AGENTS.md` rule it backs: **no live finding
closes without a scanner check, a probe assertion, a test, or a written reason.** A check id in
the table below is a literal scanner `Finding.id`, a `fixers.SUPPORTED_FINDINGS` id, or a
`probe-mod` `ProbeLog` check name (all in backticks); `tests/test_bug_class_registry.py` parses
this table and asserts every such id actually exists somewhere in the codebase. A row with no
check yet must instead read `NONE (written reason: ...)` and give the reason inline.

| Bug class | Symptom | Root cause | Check / probe assertion | Test file | First mod hit |
| --- | --- | --- | --- | --- | --- |
| VAC-CARRIER-01 | A revived carrier hull launches no fighters after the 0.98a carrier rework | `ship_data.csv` predates the `bays` column; the hull keeps a `CARRIER` hint with no bays or wings | `carrier-without-bays-or-wings`, `ship-data-missing-fighter-bays-column` | `tests/test_scanner.py` | Vacuum |
| BS-PROCGEN-01 / EX-NEWGAME-02 / EX-NEWGAME-03 | `Fatal: PlanetGenDataSpec`/`StarGenDataSpec ... not found` at new game | A custom planet/star type has no matching row in `planet_gen_data.csv`/`star_gen_data.csv` | `procgen-planet-row-missing`, `procgen-star-row-missing` | `tests/test_scanner.py`, `tests/test_fixers.py` | Broken Star |
| EX-NPE-001 / FLX-KNOWN-01 / FG-KNOWN-01 | A faction never spawns ships/fleets, or an NPE walks its known lists | A `.faction` file has an empty/missing `knownShips`/`knownWeapons`/`knownFighters` | `faction-known-lists-missing` | `tests/test_scanner.py`, `tests/test_fixers.py` | Exigency |
| EX-MARKET-01 | A market's submarket stocks fighters but not ships, or vice versa | A shipRoles/known-list migration didn't carry every role variant across | `faction-known-lists-missing`; probe assertion `submarket-stock` | `tests/test_scanner.py`, `probe-mod/src/com/bridgeforge/probe/CampaignProbeScript.java` | Exigency |
| SK-13 | `NullPointerException: Person.getPersonality() is null` in `CombatFleetManager.deployAll` once AI Tweaks is enabled | A module-hull's captain has no personality; vanilla tolerates this, AI Tweaks' extended ship AI does not | `module-captain-personality-risk`, `spawned-ship-captain-personality-risk`; probe assertion `combat-captain-personality` | `tests/test_scanner.py`, `probe-mod/src/com/bridgeforge/probe/BfProbeCombatPlugin.java` | SEEKER |
| SK-10 | A fighter wing that should still spawn standalone silently stops appearing after a carrier-rework migration | A wing's role/id regressed while adding the `bays` column elsewhere in the same hull family | `wing-role-assault-removed`, `carrier-without-bays-or-wings` | `tests/test_scanner.py`, `tests/test_boot_test.py` | SEEKER |
| LOA-RING-01 | `RingBand.writeReplace: JSON does not allow non-finite numbers` at new game | A ring/orbit radius or period is computed without a guard against a non-finite result | `orbit-period-zero`, `orbit-period-computed-unguarded`; probe assertion `rings-orbits` | `tests/test_scanner.py`, `probe-mod/src/com/bridgeforge/probe/CampaignProbeScript.java` | Legacy of Arkgneisis |
| LOA-VAR-01 | A weapon silently vanishes from a variant at load, with no error | The variant mounts a weapon whose slot type doesn't match the hull's declared slot type at that mount | `variant-weapon-slot-mismatch` | `tests/test_scanner.py` | Legacy of Arkgneisis |
| FG-DESC-01 | A ship/weapon description renders truncated or with a stray quote | A `descriptions.csv` row's quoting doesn't round-trip through the CSV parser | `description-missing`, `csv-row-extra-columns` | `tests/test_scanner.py`, `tests/test_fixers.py` | FlowerGod |
| EC-LVL-01 | A revived `LevelupPlugin` silently overrides XP progression for every mod in the load order | The target-engine `LevelupPlugin` interface gained a mandatory method the old implementation never provided | `target-interface-method-missing` | `tests/test_scanner.py` | Edmund's Church 2.5 |
| EX-REFLECT-01 | `SecurityException` at class-load time inside the RC8 script sandbox | Reflection/`File`/NIO API use, which RC8's sandbox forbids at class-load | `script-sandbox-forbidden-api`; jar-audit reflection findings | `tests/test_scanner.py`, `tests/test_jar_audit.py` | Exigency |
| EX-ROUTE-01 | A moving custom entity (e.g. Avesta) drifts far from its intended waypoints | Hard-coded hyperspace coordinates that don't scale with a changed hyperspace size | `hardcoded-hyperspace-coordinates`, `hardcoded-terrain-grid-size` | `tests/test_scanner.py` | Exigency |
| EX-BUNDLE-01 / OMG-BUNDLE-01 | A rebuilt jar silently carries dozens of extra library or vanilla classes | `javac` compiled a library's (or vanilla's) sources off the classpath into the mod's own jar | `bundled-library-classes`, `vanilla-class-duplicated-in-jar` | `tests/test_scanner.py`, `tests/test_jar_audit.py` | Exigency |
| EX-MASK-02 | A procedurally-placed nebula/mask lands in the wrong spot relative to a named system after a resolution change | A hyperspace mask was rescaled for size but never verified for placement -- this is an evidence-tier-T3 visual judgment, not something a static or probe check can prove | NONE (written reason: needs a human's eye on where the cleared region actually renders relative to Tasserus; tracked as `ROADMAP.md`'s post-1.0 "campaign-state coupling" research track, and as risk 1 in `docs/REVIVAL_ASSURANCE_PLAN.md` §5) | -- | Exigency |
| PRB-MISSION-01 | `Fatal: Error loading [data/missions/<id>/mission_text.txt] resource, not found` before the main menu (dialog only, never in the log) | A mission in `mission_list.csv` lacks one of the files every vanilla mission ships (`descriptor.json`, `MissionDefinition.java`, `mission_text.txt`, the declared icon) | `mission-required-file-missing` | `tests/test_mission_files.py`, `tests/test_probe_mod_build.py` | bridgeforge-probe 0.2.0 (first live run, 2026-09-13) |
| PRB-MISSION-02 | `Fatal: Error loading [data.missions.<id>.MissionDefinition] ... Assignment conversion not possible from type "java.lang.Object" to type "java.lang.String"` before the main menu | Loose scripts are compiled at runtime by Janino, which ignores generics: a for-each over a generic `Map.Entry<String, String>` reads its element as `Object`. The build had only checked the file with `javac`. | `loose-script-janino-risk`; triage FATAL rules "Janino script compile error" and "Script class load error"; the probe build now also compiles the loose mission with the game's own Janino | `tests/test_janino_checks.py`, `tests/test_probe_mod_build.py` | bridgeforge-probe 0.2.0 (first live run, 2026-09-13) |
| PRB-COMMON-01 | The probe logs `BF-PROBE disabled: no rig marker` and `bf_probe_config common file not found`; no setups apply and the combat mission has nothing to deploy | Starsector's common-file API appends `.data` to every name, but BridgeForge wrote the bare `bf_probe_rig`/`bf_probe_config` | NONE (written reason: a naming fact about the game's API, not a mod defect; enforced by `tests/test_probe_common_files.py`, which requires each on-disk name to be the Java `ProbeFiles` name plus `.data`) | `tests/test_probe_common_files.py`, `tests/test_probe_config.py` | bridgeforge-probe 0.2.0 (first live run, 2026-09-13) |
| PRB-COMBAT-01 | The combat probe mission runs and ends, but logs no ships, so SK-13-class captain checks never run | `EveryFrameCombatPlugin.init()` runs before any ship spawns, so iterating `engine.getShips()` there sees nothing | probe assertion `combat-deployment` (with a `combat-summary` count) | `probe-mod/src/com/bridgeforge/probe/BfProbeCombatPlugin.java` | bridgeforge-probe 0.2.0 (live run PRB-1, 2026-09-13) |
| PRB-CAMPAIGN-01 | The campaign probe applies its setups but never runs a check; the `campaign-armed` heartbeat repeats every frame | A `-1` sentinel for "not started" (`startTimestamp < 0`), but the campaign clock's timestamp can itself be negative, so the script re-armed every frame | probe assertion `campaign-armed` (it must appear once per load) | `probe-mod/src/com/bridgeforge/probe/CampaignProbeScript.java` | bridgeforge-probe 0.2.0 (live run PRB-1b, 2026-09-13) |
| PRB-FIGHTER-01 | In the probe combat mission, some of the mod's "ships" enter as a vanilla Nebula starliner (`hull=nebula`) | The probe treated fighter hulls with blank `ship_data.csv` hints as ships and deployed their fighter variants as SHIPs; the game substituted a default hull. The real size is the `.ship` file's `hullSize` | NONE (written reason: a probe-config selection bug, not a mod defect; enforced by `tests/test_probe_hull_inventory.py`) | `tests/test_probe_hull_inventory.py` | Exigency (Tarujan/Azata/Naxos; live run PRB-1b, 2026-09-13) |
| PRB-PLANET-01 | The campaign probe reports every vanilla planet as `planet-specs FAIL` ("No PlanetSpecAPI resolves for type [star_yellow]"), 1,010 false failures that bury real ones | The probe looked planet types up with `SettingsAPI.getSpec(PlanetSpecAPI.class, …)`, which never resolves them; types are keyed by `PlanetSpecAPI.getPlanetType()` from `getAllPlanetSpecs()` | probe assertion `planet-specs` (one summary line per run, FAIL lines only for real unresolved types) | `probe-mod/src/com/bridgeforge/probe/CampaignProbeScript.java` | bridgeforge-probe 0.2.0 (live run PRB-1c, 2026-09-13) |
| PRB-SAVE-01 | `save-content` reports a healthy save as `WILL_FAIL` with 50+ "missing" ids; `save-diff` against `campaign.xml.bak` shows no differences | Wing ids were read from variant file names instead of `wing_data.csv`; faction-relation keys and runtime-created ids were treated as data ids by prefix; an explicit `.bak` path was mapped back to `campaign.xml` | NONE (written reason: a save-tool false positive, not a mod defect; enforced by `tests/test_save_content_false_positives.py`) | `tests/test_save_content_false_positives.py` | Exigency save (live run PRB-2, 2026-09-13) |
| SEEKER-STATIC-01 | `Fatal: Cannot invoke ShipSystemAPI.isActive() because ShipAPI.getSystem() is null` in `ART_thrusterRotation.advance` during a battle | A per-weapon `EveryFrameWeaponEffectPlugin` kept `static ShipAPI ship` / `static ShipEngineControllerAPI engines`. Vector-cruiser debris spawned mid-battle (by `ART_phasedMainframe`) overwrote them, so a living cruiser's flame weapon read a ship with no system | `weapon-effect-static-combat-state`; triage FATAL rule "Combat loop exception"; crash attribution names SEEKER | `tests/test_weapon_effect_static_state.py`, `tests/test_triage_combat_fatal.py` | SEEKER (live run SK13-1, 2026-09-13) |
| PRB-FLEET-01 | The probe's `spawn-fleet` setup never spawns a fleet: `role 'combat' unavailable` | `pickShipAndAddToFleet` was given the role `"combat"`, which no faction has; real roles are `ShipRoles.COMBAT_SMALL/MEDIUM/LARGE/CAPITAL` | NONE (written reason: a probe-setup bug, not a mod defect; the probe now logs `setup` OK/FAIL for every spawn, which is the live evidence) | `probe-mod/src/com/bridgeforge/probe/ProbeSetup.java` | bridgeforge-probe 0.2.0 (live run SK13-1, 2026-09-13) |
| VAC-DIALOG-01 | "Vacuum's option to check the bounty board locks you in the dialog window" (player report, 2026-09-13) | The revival's recreated station options re-populated the menu with `FireBest PopulateOptions`, which runs only one PopulateOptions rule, so vanilla's options (including Leave) could vanish. Vanilla uses `FireAll` everywhere | `rules-firebest-populate-options` | `tests/test_rules_populate_options.py` | Vacuum |
| BF-DRIFT-01 | `copy-drift`/`rig-doctor` report a mod as "no drift" and `prepare-test --sync` reports success, but the rig still runs the old jar | `copy_drift` only looked in `data`/`jars`/`graphics`/`sounds`; SEEKER declares `jar/SEEKER.jar` (singular). This would also have made `release` ship SEEKER without its jar | NONE (written reason: a BridgeForge tooling bug, not a mod defect; enforced by `tests/test_copy_drift_declared_jars.py`) | `tests/test_copy_drift_declared_jars.py` | SEEKER (found syncing the SEEKER-STATIC-01 patch, 2026-09-13) |
| SEEKER-DEATH-01 | When a Betelgeuse dies, "Betelgeuse debris-class disabled" floods the screen and the game drops to ~2 FPS | `ART_organicHull.advanceInCombat` ran its `DEATH` effect every frame while the ship was a wreck, spawning 3 debris ships and 15 projectiles per frame. Its per-ship state also lives on the single shared hull mod instance | `hullmod-instance-state` (REVIEW) | `tests/test_hullmod_instance_state.py` | SEEKER (live run SK13-1b, 2026-09-13) |
| PRB-FLEET-02 | The probe's `spawn-fleet:pirates:120` created a 40-ship fleet (`actualFP=40 members=40`) | The spawn loop summed `pickShipAndAddToFleet`'s return value as if it were fleet points; it is not | NONE (written reason: a probe-setup bug; the probe now sizes by `CampaignFleetAPI.getFleetPoints()` and logs the actual FP and member count) | `probe-mod/src/com/bridgeforge/probe/ProbeSetup.java` | bridgeforge-probe 0.2.0 (live run SK13-1b, 2026-09-13) |
| SEEKER-PERSONALITY-01 | Starting some SEEKER missions: `Fatal: Cannot invoke PersonalityAPI.getId() because Person.getPersonality() is null` on deploy (stack through AI Tweaks `ExtendedShipAI` → vanilla `BasicShipAI` → `Ship.getPersonality`) | SEEKER's missions call `getCaptain().setPersonality("suicidal")` / `("fearless")`, 0.6-era ids that RC8 doesn't define (0.7.2 already lacked them). The officer's personality stays null. AI Tweaks was on the stack only because it builds the ship AI | `personality-id-unknown` (jar or source; ids a mod's own `personalities.csv` adds count as valid); triage FATAL rule "Null officer personality (unknown personality id)" | `tests/test_personality_ids.py` | SEEKER (live run SK13-1d, 2026-09-13) |
| PRB-DEBRIS-01 | The probe combat mission shows a string of "... (Ubique debris class) disabled" messages, and real hulls are skipped at the per-side cap | The probe deployed a mod's wreck-only hulls (SEEKER's 13 "Debris" hulks) as fighting ships; they are disabled on arrival and took 13 of the 24 slots | NONE (written reason: a probe-selection bug, not a mod defect; enforced by `tests/test_probe_hull_inventory.py`) | `tests/test_probe_hull_inventory.py` | SEEKER (live run SK13-1c, 2026-09-13) |
| VT-7 (script/listener duplication) | A UI-lifecycle script re-adds itself on every open/close cycle, or on every `onGameLoad`, growing per-frame cost | `onGameLoad`/dialog-open code re-registers an `EveryFrameScript`/listener without checking it is already present | NONE (written reason: needs a save-inspect-style duplication audit of live per-entity script counts from a real save -- roadmap P3b-A and P3B_SAVE_TOOLING_RECOMMENDATIONS.md item F; not yet implemented) | -- | Void-Tec r13 |

## Durable conventions (moved out of `In operation/STATUS.md`)

- **Scope authority:** revival targets player-visible behaviour on the current API, not
  byte-for-byte preservation of removed internals (`ROADMAP.md`, `In operation/STATUS.md` §"Scope
  authority").
- **Never rebuild a shipped jar from bundled `src/`** without a class-by-class diff first --
  bundled source routinely lags the shipped jar (see `docs/THIRD_PARTY_MOD_PATCHING` conventions
  and the Arkgneisis crash-fix precedent); `jar-audit`'s removed/added-class lists are the
  authoritative check.
- **Every edit that changes shippable content gets a build-tag bump** before it goes to a rig, so
  the in-game launcher name proves which build is running.
