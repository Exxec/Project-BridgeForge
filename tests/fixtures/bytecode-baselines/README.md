# Sanitized bytecode baselines

These baselines contain aggregate inventory counts only. They must not include
JARs, class bytes, package/class names, string constants, local paths, or mod
metadata. They tune inspection and reporting behavior without placing an
upstream mod in the repository.
